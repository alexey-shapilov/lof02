ul.b-pagination.left
    - var current=0
    each val, index in ['1','2','3','4','5','Все']
        if current == index
            li.b-pagination__item= val
        else
            li.b-pagination__item
                a(href='index')= val